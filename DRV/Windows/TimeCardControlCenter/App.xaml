<Application x:Class="TimeCardControlCenter.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>
        <ResourceDictionary>
            <Color x:Key="WindowColor">#07111F</Color>
            <Color x:Key="SidebarColor">#091522</Color>
            <Color x:Key="PanelColor">#0D1B2A</Color>
            <Color x:Key="PanelRaisedColor">#122437</Color>
            <Color x:Key="BorderColor">#1D344A</Color>
            <Color x:Key="TextColor">#F4F8FC</Color>
            <Color x:Key="MutedColor">#8FA6BB</Color>
            <Color x:Key="AccentColor">#69C441</Color>
            <Color x:Key="CyanColor">#4CC9F0</Color>
            <Color x:Key="GoldColor">#DAB539</Color>
            <Color x:Key="DangerColor">#FF667A</Color>

            <SolidColorBrush x:Key="WindowBrush" Color="{StaticResource WindowColor}" />
            <SolidColorBrush x:Key="SidebarBrush" Color="{StaticResource SidebarColor}" />
            <SolidColorBrush x:Key="PanelBrush" Color="{StaticResource PanelColor}" />
            <SolidColorBrush x:Key="PanelRaisedBrush" Color="{StaticResource PanelRaisedColor}" />
            <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}" />
            <SolidColorBrush x:Key="TextBrush" Color="{StaticResource TextColor}" />
            <SolidColorBrush x:Key="MutedBrush" Color="{StaticResource MutedColor}" />
            <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}" />
            <SolidColorBrush x:Key="CyanBrush" Color="{StaticResource CyanColor}" />
            <SolidColorBrush x:Key="GoldBrush" Color="{StaticResource GoldColor}" />
            <SolidColorBrush x:Key="DangerBrush" Color="{StaticResource DangerColor}" />

            <Style TargetType="TextBlock">
                <Setter Property="FontFamily" Value="Segoe UI" />
                <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            </Style>

            <Style x:Key="Panel" TargetType="Border">
                <Setter Property="Background" Value="{DynamicResource PanelBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="16" />
                <Setter Property="Padding" Value="22" />
            </Style>

            <Style x:Key="MetricPanel" TargetType="Border" BasedOn="{StaticResource Panel}">
                <Setter Property="Margin" Value="0,0,14,0" />
                <Setter Property="MinHeight" Value="124" />
            </Style>

            <Style TargetType="Button">
                <Setter Property="Foreground" Value="#08131F" />
                <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="FontFamily" Value="Segoe UI Semibold" />
                <Setter Property="FontSize" Value="13" />
                <Setter Property="Padding" Value="18,10" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}"
                                    CornerRadius="9" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.88" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.72" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.35" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
                <Setter Property="Background" Value="{DynamicResource PanelRaisedBrush}" />
            </Style>

            <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="Foreground" Value="White" />
                <Setter Property="Background" Value="#7D2B3B" />
            </Style>

            <Style TargetType="TextBox">
                <Setter Property="Background" Value="#091522" />
                <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Padding" Value="11,8" />
                <Setter Property="CaretBrush" Value="{DynamicResource AccentBrush}" />
                <Setter Property="SelectionBrush" Value="#5969C441" />
            </Style>

            <Style TargetType="PasswordBox">
                <Setter Property="Background" Value="#091522" />
                <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Padding" Value="11,8" />
                <Setter Property="CaretBrush" Value="{DynamicResource AccentBrush}" />
            </Style>

            <Style TargetType="ComboBoxItem">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
                <Setter Property="Padding" Value="11,8" />
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBoxItem">
                            <Border x:Name="ItemBorder" Background="{TemplateBinding Background}"
                                    CornerRadius="6" Padding="{TemplateBinding Padding}" Margin="3,1">
                                <ContentPresenter VerticalAlignment="Center"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsHighlighted" Value="True">
                                    <Setter TargetName="ItemBorder" Property="Background" Value="#1B3A50" />
                                    <Setter Property="Foreground" Value="White" />
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="ItemBorder" Property="Background" Value="#234734" />
                                    <Setter Property="Foreground" Value="White" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="ItemBorder" Property="Opacity" Value="0.45" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="ComboBox">
                <Setter Property="Background" Value="#091522" />
                <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Padding" Value="11,8" />
                <Setter Property="MaxDropDownHeight" Value="320" />
                <Setter Property="ScrollViewer.CanContentScroll" Value="True" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBox">
                            <Grid>
                                <Border x:Name="ComboFrame" Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                                    <Grid>
                                        <ContentPresenter x:Name="SelectionPresenter"
                                                          Margin="11,0,36,0"
                                                          VerticalAlignment="Center"
                                                          Content="{TemplateBinding SelectionBoxItem}"
                                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                          TextElement.Foreground="{TemplateBinding Foreground}"
                                                          IsHitTestVisible="False" />
                                        <TextBox x:Name="PART_EditableTextBox" Visibility="Hidden"
                                                 Margin="10,0,36,0" Padding="0"
                                                 Background="Transparent" BorderThickness="0"
                                                 Foreground="{TemplateBinding Foreground}"
                                                 CaretBrush="{DynamicResource AccentBrush}"
                                                 VerticalContentAlignment="Center"
                                                 HorizontalContentAlignment="Left" />
                                        <Path Width="9" Height="5" Margin="0,0,13,0"
                                              HorizontalAlignment="Right" VerticalAlignment="Center"
                                              Fill="{DynamicResource MutedBrush}"
                                              Data="M 0 0 L 4.5 5 L 9 0 Z" />
                                    </Grid>
                                </Border>
                                <ToggleButton x:Name="DropDownToggle" Focusable="False" ClickMode="Press"
                                              Background="Transparent" BorderThickness="0"
                                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border Background="Transparent" />
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <Popup x:Name="PART_Popup" Placement="Bottom" AllowsTransparency="True"
                                       IsOpen="{TemplateBinding IsDropDownOpen}" Focusable="False"
                                       PopupAnimation="Fade">
                                    <Border MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                            MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                            Margin="0,4,0,0" Padding="2"
                                            Background="#0D1B2A" BorderBrush="#31506A"
                                            BorderThickness="1" CornerRadius="9">
                                        <ScrollViewer VerticalScrollBarVisibility="Auto"
                                                      HorizontalScrollBarVisibility="Disabled">
                                            <ItemsPresenter />
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsEditable" Value="True">
                                    <Setter TargetName="SelectionPresenter" Property="Visibility" Value="Hidden" />
                                    <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
                                    <Setter TargetName="DropDownToggle" Property="HorizontalAlignment" Value="Right" />
                                    <Setter TargetName="DropDownToggle" Property="Width" Value="34" />
                                </Trigger>
                                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                    <Setter TargetName="ComboFrame" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ComboFrame" Property="Background" Value="#102033" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="ComboFrame" Property="Opacity" Value="0.45" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="CheckBox">
                <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
                <Setter Property="FontSize" Value="13" />
            </Style>

            <Style x:Key="NavRadio" TargetType="RadioButton">
                <Setter Property="Foreground" Value="{DynamicResource MutedBrush}" />
                <Setter Property="FontFamily" Value="Segoe UI Semibold" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="Height" Value="44" />
                <Setter Property="Margin" Value="12,2" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="RadioButton">
                            <Border x:Name="NavBorder" CornerRadius="10" Background="Transparent" Padding="14,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="4" />
                                        <ColumnDefinition Width="34" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Border x:Name="Indicator" Width="4" Height="24" CornerRadius="2"
                                            Background="Transparent" HorizontalAlignment="Left" />
                                    <TextBlock x:Name="NavGlyph" Grid.Column="1" Text="{TemplateBinding Content}"
                                               FontFamily="Segoe MDL2 Assets" FontSize="18"
                                               VerticalAlignment="Center" Foreground="{TemplateBinding Foreground}" />
                                    <Grid x:Name="SmaNavGlyph" Grid.Column="1" Width="20" Height="20"
                                          Visibility="Collapsed" HorizontalAlignment="Left"
                                          VerticalAlignment="Center">
                                        <Path Data="M 10,1 L 17.5,5.3 L 17.5,14.7 L 10,19 L 2.5,14.7 L 2.5,5.3 Z"
                                              Stroke="{TemplateBinding Foreground}" StrokeThickness="1.5"
                                              StrokeLineJoin="Round" />
                                        <Ellipse Margin="4.5" Stroke="{TemplateBinding Foreground}"
                                                 StrokeThickness="1.5" />
                                        <Ellipse Margin="8" Fill="{TemplateBinding Foreground}" />
                                    </Grid>
                                    <Canvas x:Name="UartNavGlyph" Grid.Column="1" Width="20" Height="20"
                                            Visibility="Collapsed" HorizontalAlignment="Left"
                                            VerticalAlignment="Center">
                                        <Path Data="M 1.5,4.5 L 18.5,4.5 L 16.5,15.5 L 3.5,15.5 Z"
                                              Stroke="{TemplateBinding Foreground}" StrokeThickness="1.5"
                                              StrokeLineJoin="Round" />
                                        <Ellipse Canvas.Left="2.5" Canvas.Top="8.75" Width="2.5" Height="2.5"
                                                 Stroke="{TemplateBinding Foreground}" StrokeThickness="1" />
                                        <Ellipse Canvas.Left="15" Canvas.Top="8.75" Width="2.5" Height="2.5"
                                                 Stroke="{TemplateBinding Foreground}" StrokeThickness="1" />
                                        <Ellipse Canvas.Left="5.5" Canvas.Top="7" Width="1.5" Height="1.5" Fill="{TemplateBinding Foreground}" />
                                        <Ellipse Canvas.Left="7.5" Canvas.Top="7" Width="1.5" Height="1.5" Fill="{TemplateBinding Foreground}" />
                                        <Ellipse Canvas.Left="9.5" Canvas.Top="7" Width="1.5" Height="1.5" Fill="{TemplateBinding Foreground}" />
                                        <Ellipse Canvas.Left="11.5" Canvas.Top="7" Width="1.5" Height="1.5" Fill="{TemplateBinding Foreground}" />
                                        <Ellipse Canvas.Left="13.5" Canvas.Top="7" Width="1.5" Height="1.5" Fill="{TemplateBinding Foreground}" />
                                        <Ellipse Canvas.Left="6.5" Canvas.Top="11" Width="1.5" Height="1.5" Fill="{TemplateBinding Foreground}" />
                                        <Ellipse Canvas.Left="8.5" Canvas.Top="11" Width="1.5" Height="1.5" Fill="{TemplateBinding Foreground}" />
                                        <Ellipse Canvas.Left="10.5" Canvas.Top="11" Width="1.5" Height="1.5" Fill="{TemplateBinding Foreground}" />
                                        <Ellipse Canvas.Left="12.5" Canvas.Top="11" Width="1.5" Height="1.5" Fill="{TemplateBinding Foreground}" />
                                    </Canvas>
                                    <Grid x:Name="TimingNavGlyph" Grid.Column="1" Width="20" Height="20"
                                          Visibility="Collapsed" HorizontalAlignment="Left"
                                          VerticalAlignment="Center">
                                        <Path Data="M 1,10 L 4,10 L 6,4 L 9,16 L 12,7 L 14,10 L 19,10"
                                              Stroke="{TemplateBinding Foreground}" StrokeThickness="1.7"
                                              StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                              StrokeLineJoin="Round" />
                                    </Grid>
                                    <TextBlock Grid.Column="2" Text="{TemplateBinding ToolTip}" VerticalAlignment="Center"
                                               Foreground="{TemplateBinding Foreground}" />
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="NavBorder" Property="Background" Value="#122437" />
                                </Trigger>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
                                    <Setter TargetName="NavBorder" Property="Background" Value="#152A3D" />
                                    <Setter TargetName="Indicator" Property="Background" Value="{DynamicResource AccentBrush}" />
                                </Trigger>
                                <Trigger Property="Tag" Value="Sma">
                                    <Setter TargetName="NavGlyph" Property="Visibility" Value="Collapsed" />
                                    <Setter TargetName="SmaNavGlyph" Property="Visibility" Value="Visible" />
                                </Trigger>
                                <Trigger Property="Tag" Value="Uart">
                                    <Setter TargetName="NavGlyph" Property="Visibility" Value="Collapsed" />
                                    <Setter TargetName="UartNavGlyph" Property="Visibility" Value="Visible" />
                                </Trigger>
                                <Trigger Property="Tag" Value="Timing">
                                    <Setter TargetName="NavGlyph" Property="Visibility" Value="Collapsed" />
                                    <Setter TargetName="TimingNavGlyph" Property="Visibility" Value="Visible" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
